atom    : DIGIT | IDENTIFIER
        : LPAREN expr RPAREN

factor  : (PLUS | MINUS) atom

power   : factor (POW factor)*

term    : factor ((MUL | DIV) factor)*

expr    : term ((PLUS | MINUS) term)*

equa    : expr (EQUAL expr)*

stmt

block