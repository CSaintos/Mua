# tests.yml

name: STEM Tests

on: [push, pull_request]

permissions:
  contents: read

jobs:

  token_test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: test premake help
      working-directory: ./premake
      run : |
        ./premake --help